<UserControl x:Class="ControllerToMouse.Source.GUI.Submenus.DevicesMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="30,20,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Connected Devices" 
                       Style="{StaticResource Header1Style}" 
                       Margin="0,0,0,20"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <StackPanel x:Name="ControllerCardList"/>

                    <StackPanel x:Name="EmptyStatePrompt" 
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center"
                                Visibility="Collapsed">
                        <TextBlock Text="🎮" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                        <TextBlock Text="No devices detected" 
                                   Style="{StaticResource SecondaryTextStyle}" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>

        <Border Grid.Column="1" 
                Background="{StaticResource HeaderBackgroundBrush}" 
                Padding="20" 
                BorderBrush="{StaticResource SurfaceBackgroundBrush}" 
                BorderThickness="1,0,0,0">

            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top" 
                           Text="Actions" 
                           Style="{StaticResource Header2Style}" 
                           Margin="0,5,0,20"/>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="ActionList" />
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>