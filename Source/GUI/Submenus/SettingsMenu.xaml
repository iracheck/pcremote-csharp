<UserControl x:Class="ControllerToMouse.Source.GUI.Submenus.SettingsMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Padding="0 20 0 20" 
                Background="#1e1e1e">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" VerticalAlignment="Top" Margin="0 40 0 0">

                    <Button Style="{StaticResource TabButtonStyle}" Content="GENERAL" Margin="0 5"/>
                    <Button Style="{StaticResource TabButtonStyle}" Content="ADVANCED" Margin="0 5"/>
                    <Button Style="{StaticResource TabButtonStyle}" Content="DANGER ZONE" Margin="0 5"/>

                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Bottom" Margin="0 0 0 20">
                    <Separator Margin="10 10 10 20" Background="#333333"/>
                    <Button Style="{StaticResource TabButtonStyle}" Content="ABOUT"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Padding="30 40 30 30">
            <TextBlock Text="Content Area for Selected Setting" 
                       Foreground="#cccccc" 
                       FontSize="18" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
        </Border>

    </Grid>
</UserControl>