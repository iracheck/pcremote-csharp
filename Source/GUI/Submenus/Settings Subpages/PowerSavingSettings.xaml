<UserControl x:Class="ControllerToMouse.Source.GUI.Submenus.Settings_Subpages.PowerSavingSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             mc:Ignorable="d"  
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- 1. ScrollViewer wraps all content to enable scrolling if the window is small -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <TextBlock Style="{StaticResource Header1Style}" Margin="0 0 0 20">Power Saving Settings</TextBlock>

            <!-- ============================================== -->
            <!-- GROUP 1:  -->
            <!-- ============================================== -->
            <Border>
                <StackPanel>
                    <TextBlock Style="{StaticResource Header2Style}" Margin="0 0 0 10">Sleep Mode</TextBlock>

                    <!-- Setting: Theme Selection -->
                    <Grid Margin="0 5 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Application Theme" />
                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Choose the visual appearance of the application." />
                        </StackPanel>

                        <!-- Theme Control -->
                        <ComboBox Grid.Column="1" Width="150" HorizontalAlignment="Right">
                            <ComboBoxItem Content="Dark (Default)"/>
                            <ComboBoxItem Content="Light"/>
                            <ComboBoxItem Content="System Default"/>
                        </ComboBox>
                    </Grid>

                </StackPanel>


            </Border>



            <!-- ============================================== -->
            <!-- GROUP 2: APPLICATION BEHAVIOR -->
            <!-- ============================================== -->
            <Border>
                <StackPanel>
                    <TextBlock Style="{StaticResource Header2Style}" Margin="0 20 0 10">App Behavior</TextBlock>

                    <!-- Setting: Open on Startup -->
                    <Grid Margin="0 5 0 15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Launch on System Startup" />
                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Automatically start the application when you log into Windows." />
                        </StackPanel>

                        <CheckBox Grid.Column="1" VerticalAlignment="Center" x:Name="StartUpCheckbox"/>
                    </Grid>

                    <!-- Setting: Minimize to Tray -->
                    <Grid Margin="0 0 0 15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Minimize to System Tray" />
                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Hides the application window to the tray instead of the taskbar when minimizing or closing." />
                        </StackPanel>

                        <CheckBox Grid.Column="1" VerticalAlignment="Center" x:Name="MinimizeCheckbox"/>
                    </Grid>

                    <!-- Setting: Check for Updates -->
                    <Grid Margin="0 0 0 15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Check for Updates" />
                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Automatically check for new application versions on launch." />
                        </StackPanel>

                        <!-- Checkbox for CheckForUpdates -->
                        <CheckBox Grid.Column="1" VerticalAlignment="Center" x:Name="CheckForUpdatesCheckbox"/>
                    </Grid>

                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>