<UserControl x:Class="ControllerToMouse.Source.GUI.Submenus.DevicesMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControllerToMouse.Source.GUI.Submenus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="1"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column ="0" Margin="10 20 10 10">
            <TextBlock Style="{StaticResource Header2Style}">Connected Devices</TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ControllerCardList" Orientation="Vertical"/>
            </ScrollViewer>

            <Button Style="{StaticResource SurfaceButtonStyle}" Click="Refresh_Click">Test</Button>
        </StackPanel>

        <Rectangle Grid.Column="1" Margin="0 10" Fill="{StaticResource SurfaceBackgroundBrush}"></Rectangle>

        <StackPanel Grid.Column ="2" Margin="10 20 0 0">
            <TextBlock Style="{StaticResource Header2Style}">Actions</TextBlock>
        </StackPanel>

    </Grid>
</UserControl>
